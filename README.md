# Docker


Port Mapping (-p)
```
docker run -p 80:80 nginx
```

Run In Background (-d)
```
docker run -d -p 80:80 nginx
```

Show Docker Process
```
docker ps
```

All Command
```
docker build
docker run
docker ps
docker stop
docker logs
docker exec
docker images
docker compose up
docker compose down
```
